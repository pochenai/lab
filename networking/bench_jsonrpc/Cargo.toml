[package]
name = "bench_jsonrpc"
version = "0.1.0"
edition = "2024"

[dependencies]
base64 = "0.22"
hmac = "0.12"
serde = { version = "1", features = ["derive"] }
serde_json = { version = "1", features = ["raw_value"] }
sha2 = "0.10"
