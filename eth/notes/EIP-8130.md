# Create entry why from == expected is need?
8130希望create地址的同时创建owners，所以没有使用CREATE2的方式来创建合约。
如果不指定from，其实可以把部署的地址写到expected，而不是from也可以，安全语义是等价的；
不过这样的话payer签名的时候需要先计算expected地址，才能签名，不然就容易被replay用来签名创建其他合约。
所以加上from==expected之后，就省掉payer端的派生开销，安全语义上也没问题。